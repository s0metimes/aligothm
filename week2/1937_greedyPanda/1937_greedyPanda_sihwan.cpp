#include<stdio.h>

#define MAXN 500
#define MAXTREE 1000000

int main(int argc, char **argv) {
    int i, j, n, arr[MAXN][MAXN];

    scanf("%d", &n);

    for(i = 0; i < n; i++)
        for(j = 0; j < n; j++)
            scanf("%d", &arr[i][]);

    return 0;
}